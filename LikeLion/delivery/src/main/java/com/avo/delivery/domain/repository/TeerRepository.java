package com.avo.delivery.domain.repository;

import com.avo.delivery.domain.entity.Teer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TeerRepository extends JpaRepository<Teer, Long> {
}
